#include <stdio.h>
#include <conio.h>
int main()
{
    char  c[] = "m"; // Symbol for pounds sterling
    printf("Character: %s Code: %d \n", c, c);
    gets(c);
    printf("%s",c);
    getch();
    return 0;
}